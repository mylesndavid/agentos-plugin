# Copy to .env and fill in your AgentOS Personal Access Token.
# Get one from https://tryagentos.net (Settings → Tokens).
# This value is read by .mcp.json as ${AGENTOS_TOKEN}. NEVER commit the real token.
AGENTOS_TOKEN=agentos_pat_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
